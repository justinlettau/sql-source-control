services:
  - mssql2017

install:
  - npm install

build_script:
  - ps: .\appveyor\setup.ps1
  - npm run build
  - npm link
  - ssc pull -c ./appveyor/config.json

test_script:
  - npm run test:appveyor
